
function StudentClassroomDetails() {
    // use api.get("classrooms/:classroomCode") to fetch the classroom details

    return (
        <div>
            <h1>Student Classroom Details</h1>
        </div>
    );
}

export default StudentClassroomDetails;