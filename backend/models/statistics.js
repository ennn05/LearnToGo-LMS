import sql from "../db.js";

export const countTotalNumCourses = async () => {
    const count = 0; // dummy values for now
}

export const countTotalNumCoursesByInstructor = async (instId) => {
    const count = 0; // dummy values for now
}

export const avgNumLessonsPerCourse = async () => {
    const avg = 0; // dummy values for now
}

export const avgNumLessonsPerCourseByInstructor = async (instId) => {
    const avg = 0; // dummy values for now
}